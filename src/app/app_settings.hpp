#pragma once

// Window defaults are application policy, shared by the frame loop and UI.
namespace AppSettings {
const int kWindowWidth = 1280;
const int kWindowHeight = 720;
} // namespace AppSettings
