#pragma once

// Window defaults and release identity are application policy, shared by the
// frame loop and UI.
namespace AppSettings {
const int kWindowWidth = 1280;
const int kWindowHeight = 720;
const char kVersion[] = "v0.2.2-alpha.5.1";
} // namespace AppSettings
